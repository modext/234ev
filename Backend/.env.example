# Copy this to .env for local dev. On Railway, set these in the service Variables tab instead.

# Railway auto-generates this when you add a PostgreSQL plugin to your project.
DATABASE_URL="postgresql://user:password@localhost:5432/234ev?schema=public"

PORT=3001
